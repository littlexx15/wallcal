# v1.5.8

- 显示设置新增日历显示屏幕，默认主屏幕；只更新选定屏幕。
- 按选定屏幕尺寸生成日历，自动避让换算屏幕坐标。
- 分屏备份与恢复，切换屏幕恢复原日历屏幕；断开后暂停更新。
- 需要扩展桌面模式；Windows 平铺/跨区背景请先改为填充。

# v1.5.7

- 支持导入旧版原始 data.json，沿用完整备份的验证、导入前备份及同步暂停机制。
- 自动布局失败时明确提示，可选择切换全屏。
- 包含 v1.5.6 单次事项改期功能。

# v1.5.6

- 编辑单次事项时可修改日期，保存后跳到目标日期并刷新壁纸，保留完成状态。
- 重复事项日期暂不开放修改；编辑重复事项不再意外改变系列起始日期。

# v1.5.5

- 首次应用前备份静态原壁纸及填充方式，无法读取原图时取消更换。
- 提供手动补设原壁纸入口，帮助未留备份的旧版用户恢复。
- 发布压缩包包含运行须知和卸载恢复说明。
- 汇总 v1.4–v1.5 的可靠性、导出备份、自定义假期、图片主题、自动布局、大字号和滚轮修复。

# v1.5.4

- 修复滚动显示设置时误改背景透出程度：滑杆仅响应点击与拖动，滚轮交给页面。

# v1.5.3

- 长事项最多两行，减少行间距，优先显示更多事项并保留溢出提示。
- 主题保留护眼、墨夜、自定义图片；布局保留自动避让与全屏，旧设置自动迁移。
- 包含每 2 秒后台检查与稳定 1 秒后更新的图标避让优化。

# v1.5.2

- 图标位置后台每 2 秒检查，变化后间隔 1 秒确认稳定再重绘。
- 未变化不重绘，连续变化合并，复用已确认坐标避免重复扫描。

# v1.5.1

- 图片背景默认日历底板 65% 不透明度、顶部 80%，减轻日期格遮挡。
- 显示设置新增背景透出滑杆和延迟预览；输入窗口保持清晰。

# 壁历 WallCal v1.5.0

- 新增自动避让桌面图标：按图标位置和安全间距寻找可用区域，后台检查位置变化。
- 新增图片主题：取色、浅色/深色预览、强调色微调、命名保存和切换。
- 统一窗口、输入框、按钮、列表及壁纸主题；换主题保留未提交文字。
- 全量备份包含各个图片主题的背景资源；本机图片主题不随 Gist 同步。
- 主桌面无足够空间或 Windows 暂时无法提供图标位置时提示用户，保留原壁纸。

---

# 壁历 WallCal v1.4.2

- 将「右侧四分之三」替换为「右侧五分之四」，日历占可用宽度的 80%。
- 原四分之三设置和旧备份自动迁移为五分之四，其余布局保持不变。

---

# 壁历 WallCal v1.4.1

- 修复普通窗口下备忘输入区被排到日历下方的问题：紧凑日历在左，输入区固定在右上方，事项列表独立滚动。
- 工具栏只在布局切换时重排，日期按钮和事项卡片复用，减少切换日期及拖动窗口时的重绘。
- 新增「右侧四分之三」壁纸布局，原有布局继续保留，支持备份恢复。
- 84 条合成事项、连续 20 次选择日期的本机测试：响应中位数从约 372 ms 降至 47 ms。实际耗时随设备及事项数量变化。
- 增加普通窗口和大字号下输入框、提交按钮首屏可见的回归检查。

运行 `WallCal-1.4.1.exe`，先退出正在运行的旧版。数据目录保持不变。

---

# 壁历 WallCal v1.4.0

## 可靠性

- 兼容中文冒号、全角数字时间输入。
- 启动、跨天及屏幕工作区变化时刷新；唤醒后检查日期。
- 壁纸刷新串行合并，完整写入图片后应用，检查 Windows API 返回结果，不再删除系统壁纸缓存。
- 完成勾改为图形绘制；每天重复事项也显示在日期格。
- 开机启动改用支持 Unicode 路径的 Windows Run 配置。
- 窗口适配任务栏工作区，保存位置尺寸，小屏自动上下排列并支持滚动。
- 新增停止更新与原静态壁纸恢复；旧版已覆盖且没有备份的原图需手动选择。

## 桌面体验

- 右侧约三分之二、紧凑靠右、全屏三种布局。
- 标准、大、特大壁纸字号，以及独立界面缩放。
- 高对比度文字、今天高亮、过去日期淡化。
- 自定义背景本地副本，重启或更新事项仍保留。

## 实用功能

- 支持日期范围的年假、休息、上班标记与自定义名称。
- 日期范围 CSV 导出，包含重复事项及各日完成状态。
- 全量 JSON 备份、背景随备份保存、恢复前自动备份；恢复后暂停自动云同步。
- 损坏数据不再直接覆盖为空数据，保留原文件供恢复。

## 使用

运行 `WallCal-1.4.0.exe`；升级前关闭旧版。现有数据仍位于 `%APPDATA%\WallCal`。
新版修复了黑屏相关的缓存处理与并发风险；不同电脑的第三方壁纸软件和休眠场景仍需实际验证。
